import { html } from "@talkjs/web-components";
/** @import { MessageListFooterProps } from "@talkjs/web-components"; */

/**
 * MessageListFooter is shown below the message list, above the message field. It does not scroll with the message list.
 *
 * @param {MessageListFooterProps} props
 */
export function MessageListFooter(props) {
  return null;
}
